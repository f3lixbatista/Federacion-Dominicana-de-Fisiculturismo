

<nav class="navbar navbar-expand-md py-0 navbar-dark bg-dark">
<!-- <% console.log("DEPURACIÓN NAVBAR - Usuario:", locals.user) %>
  <pre class="text-white small">Rol detectado: <%= locals.user ? user.role : 'Nadie logueado' %></pre>
  -->
  <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse mx-2" id="navbarSupportedContent">
    <ul class="navbar-nav w-100 d-flex justify-content-between align-items-center">
      
      <!-- INICIO: Visible siempre -->
      <li class="nav-item">
        <a class="navbar-brand py-0" href="/"><strong>Inicio</strong></a>
      </li>

      <!-- Ejemplo de enlace en Navbar -->
<li class="nav-item">
    <a class="nav-link fw-bold" href="/eventos/competencias">
        <i class="fas fa-calendar-star"></i> COMPETENCIAS
    </a>
</li>
      
      <!-- Sección Pública (Visible para todos) -->
      <li class="nav-item">
          <a class="nav-link fw-bold text-primary" href="/eventos/historico">
              <i class="fas fa-award"></i> SALÓN DE LA FAMA
          </a>
      </li>


      <% if (locals.user) { %>
        <!-- PÚBLICO / GENERAL (Logueados) -->
        <li class="nav-item"><a class="nav-link" href="/social"><strong>Social</strong></a></li>
        <li class="nav-item"><a class="nav-link" href="/noticias"><strong>Noticias</strong></a></li>

        <!-- ATLETAS -->
        <% if (user.role === 'atleta' || user.role === 'admin') { %>
          <li class="nav-item"><a class="nav-link" href="/servicios"><strong>Servicios</strong></a></li>
          <li class="nav-item"><a class="nav-link" href="/afiliacion"><strong>Afiliacion</strong></a></li>
        <% } %>

        <!-- Sección Administrativa (Solo Admin/Ejecutivo) -->
        <% if (user && (user.role === 'admin' || user.role === 'ejecutivo')) { %>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle bg-dark text-white rounded px-3" href="#" id="adminMenu" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <i class="fas fa-shield-alt"></i> PANEL DE CONTROL
                </a>
                <ul class="dropdown-menu shadow-lg" aria-labelledby="adminMenu">
                    <% if (locals.evento && evento.id) { %>
                        <li><a class="dropdown-item fw-bold" href="/eventos/<%= evento.id %>/centro-mando"><i class="fas fa-broadcast-tower me-2"></i> CENTRO DE MANDO</a></li>
                    <% } %>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/atletas"><i class="fas fa-users me-2"></i> Gestión de Atletas</a></li>
                    <li><a class="dropdown-item" href="/preparadores"><i class="fas fa-user-tie me-2"></i> Gestión de Coaches</a></li>
                    <li><a class="dropdown-item" href="/categorias"><i class="fas fa-tags me-2"></i> Gestión de Categorías</a></li>
                    <li><a class="dropdown-item" href="/categorias/nuevoEvento"><i class="fas fa-calendar-plus me-2"></i> Crear Evento</a></li>
                </ul>
            </li>
        <% } %>

        <!-- JUECES -->
        <% if (user.role === 'juez') { %>
          <li class="nav-item"><a class="nav-link" href="/jueces"><strong>Jueces</strong></a></li>
        <% } %>

        <!-- FOTÓGRAFO -->
        <% if (user.role === 'fotografo') { %>
            <li class="nav-item"><a class="nav-link" href="/fotografia/upload"><strong>Fotografía</strong></a></li>
        <% } %>

        <!-- BOTÓN SALIR -->
        <li class="nav-item">
          <a class="nav-link text-danger" href="/logout" title="Cerrar Sesión de <%= user.role %>">
            <strong>Salir</strong>
          </a>
        </li>

      <% } else { %>
        <!-- SI NO HAY SESIÓN -->
        <li class="nav-item">
          <a class="nav-link text-info" href="/login"><strong>Entrar / Registrarse</strong></a>
        </li>
      <% } %>

      <!-- BUSCADOR -->
      <form class="form-inline my-2 my-lg-0 d-none d-lg-flex">
        <input class="form-control form-control-sm mr-sm-2" type="search" placeholder="Buscar..." aria-label="Search">
        <button class="btn btn-outline-success btn-sm my-2 my-sm-0" type="submit">Go</button>
      </form>
    </ul>
  </div>
</nav>
